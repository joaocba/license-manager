import Header from "../Layout/Header";

const Settings = () => {
    return (
        <>
            <Header title="Settings" subtitle="Manage your dashboard settings here" />
        </>
    );
};

export default Settings;
