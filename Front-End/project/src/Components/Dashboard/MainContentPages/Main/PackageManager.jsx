import Header from "../Layout/Header";

const PackageManager = () => {
    return (
        <>
            <Header title="Package Manager" subtitle="Manage your packages here" />
        </>
    );
};

export default PackageManager;
